https://github.com/mackron/miniaudio


ecs_system
{
        scenes
        {
                entities
        };
       transform_componnets[100000];

}

scene add entity(ecs_system, scene, player)
{
        scene.entitites[i] = player;
}


entities not a pointer inside scene




collision detection
Pixel perfect collision based on alpha channel of a PNG image
 
TODO: KAN BETER: 
if (e->physics_component.collided_with[context->player_hitbox] && key_being_pressed(w, KEY_J))
        scene->data_components[e].active = false;  */


ecs scene serialization

animation looping andd ssingle play

ui

texture = texturefromfile;

children entities

delete children

dit kan blijkbaar: scene.script_components[player].context = &(player_script_context_t){ .hitbox = player_hitbox };